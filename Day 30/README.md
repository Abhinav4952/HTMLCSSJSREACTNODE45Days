# Day 30 — Regular Expressions: Flags, Literals, Classes, Quantifiers

## Learning objectives

- Use `RegExp` and regex literals with common **flags**.
- Match digit/letter classes and combine quantifiers.
- Choose between greedy vs non-greedy where it matters (preview for Day 33).

## Prerequisites

- **Day 29** and earlier JavaScript days.

## How to use this folder

1. Read `DAY-30-THEORY-AND-REFERENCE.md`.
2. Run tasks in order with Vitest.

## Task list

| Order | Folder | Type | Verify with |
|------:|--------|------|-------------|
| 1 | `task-01-regex-test-flags` | JavaScript | `npm install` → `npm test` |
| 2 | `task-02-digit-letter-classes` | JavaScript | `npm install` → `npm test` |
| 3 | `task-03-quantifiers-basics` | JavaScript | `npm install` → `npm test` |
| 4 | `task-04-extract-yaml-ish-keys-challenge` | JavaScript | `npm install` → `npm test` |
