# Evaluation Criteria — Day 12 — Task 01 — Semantic Skip Link + Article Page

## Scope

Graded: `index.html` landmark structure + `styles/main.css` skip-link behavior.  
Out of scope: JavaScript frameworks.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 45 | Landmarks valid; skip link works |
| Requirements coverage | 35 | All TODOs removed; content present |
| UX / accessibility | 20 | Focus order + visible focus styles |

## Pass / fail gates

- Any `TODO(Day12-task01)` left in submitted files.
- More than one visible `main` or missing `id="main"` target for skip link.

## Evidence the grader should collect

- Short screen recording or step-by-step notes proving skip link focus behavior.

## AI-assisted grading prompt (optional)

```markdown
## AI grading prompt (paste into your grader)
You are evaluating a graduate-level assignment. Use only observable criteria below.
Repository context: `Day 12/practical-tasks/task-01-semantic-skip-nav-article-page`.
Candidate changed files: `index.html`, `styles/main.css`.
Rubric: confirm TODOs removed; confirm single `main` with `id="main"`; confirm skip link exists and targets `#main`; confirm header/nav/article/aside/footer structure; confirm skip link becomes visible on keyboard focus.
Return: Pass/Fail, score table, blocking issues, non-blocking suggestions.
```
