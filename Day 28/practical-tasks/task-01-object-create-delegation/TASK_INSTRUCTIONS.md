# Task 1 — Delegation with Object.create

## Summary

Use `Object.create` to share methods via the prototype chain.

## Learning goals

- Prototype delegation vs copying properties.

## Provided files

- `package.json`, `.npmrc`
- `src/` (your work), `tests/` (read-only)

## Prerequisites

- Day 28 theory file.

## What you will implement

1. Returned object inherits `label` from `noiseMethods`.

2. Each instance stores its own `kind`.

## Constraints

- Do not edit tests.

## How to run and verify

```bash
cd "Day 28/practical-tasks/task-01-<this-folder-slug>"
npm install
npm test
```

## `TODO` map

| TODO | Done means |
|------|------------|
| See `src/` | All `TODO(Day28-task01)` resolved |

## Submission checklist (Git)

- [ ] `npm test` passes
