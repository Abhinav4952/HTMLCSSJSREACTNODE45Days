# Day 28 — Prototypes: `Object.create`, Chains, and `instanceof`

## Learning objectives

- Explain **prototype links** created by `Object.create` and constructors.
- Use **delegation** to share behavior without copying methods onto every object.
- Apply `instanceof` with the mental model of **constructor.prototype** checks.

## Prerequisites

- Prior JavaScript days through **Day 27** (see theory cross-links).

## How to use this folder

1. Read `DAY-28-THEORY-AND-REFERENCE.md`.
2. Complete tasks in `practical-tasks/` in order (`npm install` → `npm test`).

## Task list

| Order | Folder | Type | Verify with |
|------:|--------|------|-------------|
| 1 | `task-01-object-create-delegation` | JavaScript | `npm install` → `npm test` |
| 2 | `task-02-prototype-chain-lookup` | JavaScript | `npm install` → `npm test` |
| 3 | `task-03-instanceof-constructors` | JavaScript | `npm install` → `npm test` |
| 4 | `task-04-delegate-chain-challenge` | JavaScript | `npm install` → `npm test` |
