# Task 2 — Safe JSON parse

## Summary

Wrap `JSON.parse` with `try/catch`.

## Learning goals

- Handle `SyntaxError` specifically if you like—but tests only require fallback behavior.

## Provided files

- `package.json`, `.npmrc`, `src/`, `tests/`

## Prerequisites

- Day 32 theory.

## What you will implement

1. Non-strings return fallback.

## Constraints

- Do not edit tests.

## How to run and verify

```bash
cd "Day 32/practical-tasks/task-02-<folder-slug>"
npm install
npm test
```

## Submission checklist (Git)

- [ ] `npm test` passes
