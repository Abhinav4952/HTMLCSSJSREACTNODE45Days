# Task 02 — Inline Semantics and Lists

## Summary

Mark up a **cheat sheet** page that uses inline semantics (`<strong>`, `<em>`, `<code>`, `<kbd>`, `<mark>`, `<abbr>`) and three list types (`ul`, `ol`, `dl`).

## Learning goals

- Match element choice to meaning, not default styling.
- Use `<dl>` for term/definition pairs.

## Provided files

- `index.html`, `styles/main.css`

## What you will implement

Complete all `TODO(Day02-task02)` markers with **original** list items (not copied from the instructions).

## How to run and verify

Browser view + screen reader optional (VoiceOver/NVDA) to hear emphasis.

## `TODO` map

| TODO | Done means |
|------|------------|
| strong/em | At least one each used meaningfully |
| code/kbd | At least one each |
| mark | Highlight a keyword in a paragraph |
| abbr | One abbreviation with `title` |
| ul/ol/dl | Each present with ≥2 items/terms |

## Submission checklist

- [ ] Original list content
