# Task 01 — Conditional Branching Lab (Vitest)

## Summary

Implement **`signCategory`** with strict type checks and predictable numeric edge cases.

## Provided files

- `package.json`, `.npmrc`, `src/signsLab.js`, `tests/signsLab.test.js`

## What you will implement

Complete `// TODO(Day18-task01):` in `src/signsLab.js` so `npm test` passes.

## How to run and verify

```bash
cd "Day 18/practical-tasks/task-01-conditional-branching-lab"
npm install
npm test
```

## Constraints

- Do not edit tests.

## `TODO` map

| TODO | Done means |
|------|------------|
| `signCategory` | Matches all rules in tests |

## Submission checklist (Git)

- [ ] `npm test` passes
- [ ] `package-lock.json` exists in this folder
