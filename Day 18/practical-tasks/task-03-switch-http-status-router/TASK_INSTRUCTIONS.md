# Task 03 — `switch` HTTP Status Router (Vitest)

## Summary

Implement **`httpStatusFamily`** using a `switch` (not a chain of unrelated `if`s) to bucket HTTP codes.

## Provided files

- `package.json`, `.npmrc`, `src/switchLab.js`, `tests/switchLab.test.js`

## What you will implement

Complete `// TODO(Day18-task03):` in `src/switchLab.js`.

## Constraints

- Must include a `switch` statement in your solution (any acceptable form, including `switch (true)`).
- Do not edit tests.

## How to run and verify

```bash
cd "Day 18/practical-tasks/task-03-switch-http-status-router"
npm install
npm test
```

## `TODO` map

| TODO | Done means |
|------|------------|
| `httpStatusFamily` | Buckets + invalid rules |

## Submission checklist (Git)

- [ ] `npm test` passes
- [ ] `package-lock.json` exists in this folder
