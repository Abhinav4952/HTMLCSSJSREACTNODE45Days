// TODO(Day36-task04): Accept three props: `leftAside`, `main`, and `rightAside` (each a React node).
// TODO(Day36-task04): Render them inside the grid: left column `aside`, center `section`, right `aside`.
// Use the existing class names: outer `div.layout`, inner panels already have `panel` class from children.
export default function DashboardLayout() {
  return (
    <div className="layout">
      <p>Layout placeholder — complete TODOs.</p>
    </div>
  )
}
