// TODO(Day36-task02): Accept `title` (string) and `children` props.
// TODO(Day36-task02): Render `title` inside <h2 className="card__title">.
// TODO(Day36-task02): Render `children` inside <section className="card__body"> (replace placeholder).
export default function PostCard() {
  return (
    <article className="card">
      <h2 className="card__title">Title placeholder</h2>
      <section className="card__body">
        <p>Body placeholder</p>
      </section>
    </article>
  )
}
