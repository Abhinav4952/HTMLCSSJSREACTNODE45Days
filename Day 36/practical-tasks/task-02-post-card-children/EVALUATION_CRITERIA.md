# Evaluation Criteria — Day 36 — Task 2 — Post card

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 60 | Title + multi-paragraph children render in correct regions. |
| Code quality | 25 | Semantic tags preserved (`article`, `section`, `h2`). |
| Requirements | 15 | No hard-coded lorem from `App.jsx` inside `PostCard`. |

## Pass / fail gates

- Build fails.
- Children omitted.

## AI grading prompt

Verify `PostCard.jsx` uses `title` and `children` props correctly.
