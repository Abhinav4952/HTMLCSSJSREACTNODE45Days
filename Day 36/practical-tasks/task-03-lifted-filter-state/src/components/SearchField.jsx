// TODO(Day36-task03): Render a **controlled** `<input type="search">` using `value` from props.
// TODO(Day36-task03): On each `input` event, call `props.onChange` with the **string** value (`event.target.value`).
export default function SearchField({ value, onChange }) {
  return <p>Search field placeholder — complete TODOs.</p>
}
