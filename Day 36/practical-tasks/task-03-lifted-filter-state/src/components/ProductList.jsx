// TODO(Day36-task03): Filter `products` by `query` using case-insensitive substring match on `name`.
// TODO(Day36-task03): When `query` is empty, show all products.
// TODO(Day36-task03): Render results as a `<ul>` of `<li key={product.id}>...</li>`.
export default function ProductList({ products, query }) {
  return (
    <section>
      <p>List placeholder — complete TODOs.</p>
    </section>
  )
}
