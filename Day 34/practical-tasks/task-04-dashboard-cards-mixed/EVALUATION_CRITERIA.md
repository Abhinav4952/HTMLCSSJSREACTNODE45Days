# Evaluation Criteria — Day 34 — Task 4 — Dashboard cards (mixed)

## Scope

Graded: `MetricCard.jsx`, `TeamNote.jsx`, `App.jsx` as constrained by TODO tags.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 45 | Props render correctly in both component types. |
| Layout | 25 | `grid` wrapper present; grid visibly applied. |
| Code quality | 20 | No prop mutation; consistent semantic tags. |
| Requirements coverage | 10 | Matches specified strings from `App.jsx`. |

## Pass / fail gates

- `npm run build` fails.
- `TeamNote` converted entirely to a function component against instructions (unless instructor override).

## Evidence

- Screenshot wide + narrow viewport showing grid behavior.
- `npm run build` output snippet (success).

## AI grading prompt (paste into your grader)

Check three files for TODO completion: prop usage + `div.grid` wrapper. Return Pass/Fail.
