// TODO(Day34-task04): Accept `title` and `value` props and render them inside the <article>.
// Use <h2> for the title (metric label) and <p> for the value (large number/text).
export default function MetricCard() {
  return (
    <article className="metric">
      <h2>Metric label</h2>
      <p>000</p>
    </article>
  )
}
