/**
 * Renders a friendly one-line greeting using props.
 */
// TODO(Day34-task01): Accept `name` and optional `role` props (function parameters / destructuring).
// TODO(Day34-task01): If `name` is missing, display "Hello, guest" (default value in the signature is fine).
// TODO(Day34-task01): If `role` is provided, append it in parentheses, e.g. "Hello, Avery (student)".
export default function UserGreeting() {
  return <p>Hello, learner. (complete the TODOs in this file)</p>
}
