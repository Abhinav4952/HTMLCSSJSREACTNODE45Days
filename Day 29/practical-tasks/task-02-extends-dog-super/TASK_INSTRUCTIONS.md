# Task 2 — Dog extends Animal

## Summary

Subclass constructor must call `super` and extend `speak`.

## Learning goals

- Practice `extends` + `super` in methods.

## Provided files

- `package.json`, `.npmrc`
- `src/animal.js` (same starter as task 01 so this folder stays self-contained)
- `src/dog.js`, `tests/dog.test.js`

## Prerequisites

- Day 29 theory.

## What you will implement

1. Call `super(name)`.

2. Append breed in `speak` using `super.speak()`.

## Constraints

- Do not edit tests.

## How to run and verify

```bash
cd "Day 29/practical-tasks/task-02-extends-dog-super"
npm install
npm test
```

## Submission checklist (Git)

- [ ] `npm test` passes
