# Day 29 — ES2015 Classes: `extends` and `super`

## Learning objectives

- Declare classes with concise method syntax.
- Use **`extends`** and **`super`** for inheritance.
- Override methods while calling parent implementations when needed.

## Prerequisites

- **Day 28** and earlier JavaScript days.

## How to use this folder

1. Read `DAY-29-THEORY-AND-REFERENCE.md`.
2. Run tasks in order with Vitest.

## Task list

| Order | Folder | Type | Verify with |
|------:|--------|------|-------------|
| 1 | `task-01-class-animal-speak` | JavaScript | `npm install` → `npm test` |
| 2 | `task-02-extends-dog-super` | JavaScript | `npm install` → `npm test` |
| 3 | `task-03-instanceof-hierarchy` | JavaScript | `npm install` → `npm test` |
| 4 | `task-04-shape-area-challenge` | JavaScript | `npm install` → `npm test` |
