# Task 02 — Pseudo-element Icons + Accessibility Boundaries

## Summary

Style a checklist using `::before` for a **visual** check glyph. The checklist must remain understandable when CSS fails to load: keep the **text** in the DOM (already provided).

## Rules

- Do not put the entire task meaning only inside `content:`.
- `::before` may add a decorative prefix only.

## `TODO` map

| TODO | Done means |
|------|------------|
| checklist | `li::before` adds icon/prefix |
| spacing | comfortable alignment using flex on `li` |
