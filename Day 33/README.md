# Day 33 — Regex Advanced + Promises: Groups, Chaining, and Errors

## Learning objectives

- Use **non-greedy** quantifiers and **capturing groups**.
- Create Promises with **executors** and handle resolve/reject.
- Chain `.then` / `.catch` and propagate errors predictably.

## Prerequisites

- **Day 32** and earlier JavaScript days.

## How to use this folder

1. Read `DAY-33-THEORY-AND-REFERENCE.md`.
2. Run tasks in order with Vitest.

## Task list

| Order | Folder | Type | Verify with |
|------:|--------|------|-------------|
| 1 | `task-01-non-greedy-match` | JavaScript | `npm install` → `npm test` |
| 2 | `task-02-capture-group-replace` | JavaScript | `npm install` → `npm test` |
| 3 | `task-03-promise-constructor-basics` | JavaScript | `npm install` → `npm test` |
| 4 | `task-04-promise-chain-challenge` | JavaScript | `npm install` → `npm test` |
