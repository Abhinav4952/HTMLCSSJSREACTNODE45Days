# Task 03 — Colspan and Rowspan Timetable

## Summary

Model a **weekly timetable** with a merged header row (`colspan`) and one `rowspan` cell for a multi-hour block.

## Learning goals

- Plan spans on paper before coding.
- Keep **row/column counts** consistent.

## Provided files

- `index.html`, `styles/main.css`

## What you will implement

Complete the table described in `index.html` TODOs with **original** course/life schedule content.

## Constraints

- Minimum **5** columns (e.g. empty corner + 4 time slices) and **6** rows (header rows + 5 weekdays) — adjust if your design needs more, but not less.

## `TODO` map

| TODO | Done means |
|------|------------|
| colspan | Top header row merges time bands |
| rowspan | At least one multi-row cell |
| scope | Sensible `th` usage for row/col labels |
| caption | Explains the schedule |

## Manual checks

- [ ] Browser table layout has no “offset” cells at row ends.
