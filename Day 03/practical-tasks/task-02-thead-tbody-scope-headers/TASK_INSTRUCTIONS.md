# Task 02 — thead, tbody, and Scoped Headers

## Summary

Upgrade a starter table: move labels into `<thead>` with `<th scope="col">`, add row headers with `<th scope="row">` in `<tbody>`, and include `<tfoot>` totals row.

## Learning goals

- Use `thead`/`tbody`/`tfoot` correctly.
- Apply `scope` on column and row headers.

## Provided files

- `index.html`, `styles/main.css`

## What you will implement

Replace TODO placeholders with a complete **inventory-style** table (≥4 rows of items excluding header/tfoot).

## `TODO` map

| TODO | Done means |
|------|------------|
| structure | `caption`, `thead`, `tbody`, `tfoot` present |
| col headers | `th scope="col"` for each column in thead |
| row headers | First column in tbody uses `th scope="row"` |
| totals | `tfoot` summarizes at least one numeric column |

## Manual checks

- [ ] W3C validator shows no fatal table structure errors.
