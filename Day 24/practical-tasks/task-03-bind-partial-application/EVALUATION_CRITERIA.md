# Evaluation Criteria — Day 24 — Task 03 — `bind`

## Scope

Graded: `src/bindLab.js` vs tests.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 40 | Tests pass |
| Code quality | 20 | Small wrappers |
| Requirements coverage | 25 | Uses `bind` as required |
| Docs / readability | 15 | Matches JSDoc |

## Pass / fail gates

- Any test failure.
- `bindFirstArg` implemented with closures instead of `bind` (unless tests still pass—still fail manual gate).

## AI-assisted grading prompt (optional)

```markdown
Context: `Day 24/practical-tasks/task-03-bind-partial-application`.
Verify tests pass and both solutions use `.bind`.
```
