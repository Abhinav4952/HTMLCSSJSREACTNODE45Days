# Task 01 — Method Calls and `this`

## Summary

Implement a tiny **counter object** whose methods mutate shared state via **`this`**.

## Learning goals

- Use concise methods on an object literal with correct `this`.

## Provided files

- `src/thisBasicsLab.js`, `tests/thisBasicsLab.test.js`

## How to run and verify

```bash
cd "Day 24/practical-tasks/task-01-method-calls-and-this"
npm install
npm test
```

## `TODO` map

| TODO | Done means |
|------|------------|
| `createCounter` | `read`/`inc` operate on `this.total` |

## Submission checklist (Git)

- [ ] `npm test` passes
