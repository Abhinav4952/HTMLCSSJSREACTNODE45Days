# Evaluation Criteria — Day 24 — Task 02 — `call` / `apply`

## Scope

Graded: `src/callApplyLab.js` vs tests + grep for `.call` / `.apply`.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 40 | Tests pass |
| Code quality | 20 | Minimal glue |
| Requirements coverage | 25 | Required APIs used |
| Docs / readability | 15 | Matches JSDoc |

## Pass / fail gates

- Any test failure.
- `maxWithApply` implemented using only spread (`Math.max(...nums)`).

## AI-assisted grading prompt (optional)

```markdown
Context: `Day 24/practical-tasks/task-02-call-and-apply-borrowing`.
Verify tests pass and `maxWithApply` uses `.apply`.
```
