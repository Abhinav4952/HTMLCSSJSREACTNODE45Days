# Evaluation Criteria — Day 21 — Task 03 — Default Parameters Lab

## Scope

Graded: `src/defaultParamsLab.js` vs tests.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 40 | Tests pass |
| Code quality | 20 | Straightforward string normalization |
| Requirements coverage | 25 | Default parameters used for `separator` and `emptyFallback` |
| Docs / readability | 15 | Matches JSDoc |

## Pass / fail gates

- Any test failure.
- Defaults for `separator` / `emptyFallback` implemented only via `??` / `||` without signature defaults.

## AI-assisted grading prompt (optional)

```markdown
## AI grading prompt (paste into your grader)
Context: `Day 21/practical-tasks/task-03-default-parameters-lab`.
Verify tests pass and `buildLabel` signature includes default values for `separator` and `emptyFallback`.
```
