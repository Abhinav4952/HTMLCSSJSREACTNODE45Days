import ProfilePanel from './components/ProfilePanel.jsx'

export default function App() {
  return (
    <main>
      <h1>Profile thunk + rejection UX</h1>
      <p>Fix the thunk URL and implement `extraReducers` so successful loads render the user card.</p>
      <ProfilePanel />
    </main>
  )
}
