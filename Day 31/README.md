# Day 31 — ES2015 Collections & Objects: `Map`, `Set`, `Symbol`, and Object utilities

## Learning objectives

- Use **`Map`/`Set`** for collections with non-string keys or uniqueness.
- Apply **`Object.fromEntries`** / **`Object.entries`** patterns.
- Introduce **`Symbol`** as opaque keys (lightweight exercise).

## Prerequisites

- **Day 30** and earlier JavaScript days.

## How to use this folder

1. Read `DAY-31-THEORY-AND-REFERENCE.md`.
2. Run tasks in order with Vitest.

## Task list

| Order | Folder | Type | Verify with |
|------:|--------|------|-------------|
| 1 | `task-01-map-frequency-count` | JavaScript | `npm install` → `npm test` |
| 2 | `task-02-set-unique-normalized` | JavaScript | `npm install` → `npm test` |
| 3 | `task-03-symbol-description-key` | JavaScript | `npm install` → `npm test` |
| 4 | `task-04-weakmap-metadata-challenge` | JavaScript | `npm install` → `npm test` |
