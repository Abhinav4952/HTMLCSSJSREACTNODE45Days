# Copy to `.env` (gitignored). Never commit real credentials.
# MONGODB_URI — full SRV or local URI (from instructor/Atlas/Docker output).
MONGODB_URI=
# MONGODB_DB_NAME — logical database for this cohort (default matches code fallback).
MONGODB_DB_NAME=course-node-45
