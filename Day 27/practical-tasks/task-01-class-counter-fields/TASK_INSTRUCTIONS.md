# Task 1 — Class + instance fields

## Summary

Implement `StepCounter` with public fields `count` and `step`.

## Learning goals

- Practice class fields and constructor initialization.

## Provided files

- `package.json`, `.npmrc`
- `src/` (your work), `tests/` (read-only)

## Prerequisites

- Day 27 theory file.

## What you will implement

1. `constructor(step)` sets `this.step` when valid.

2. `tick(times)` increments `count` by `step * times`.

## Constraints

- Do not edit tests.

## How to run and verify

```bash
cd "Day 27/practical-tasks/task-01-<this-folder-slug>"
npm install
npm test
```

## `TODO` map

| TODO | Done means |
|------|------------|
| See `src/` | All `TODO(Day27-task01)` resolved |

## Submission checklist (Git)

- [ ] `npm test` passes
