# Task 01 — Static vs Relative Playground

## Summary

Practice `position: relative` offsets without removing the element from flow. You will nudge labels and create a subtle “lifted” heading using `top`/`left` **without** changing document width (no horizontal scroll).

## What you will implement

Complete `TODO(Day08-task01)` in `styles/main.css`.

## Manual checks

- [ ] No horizontal scrollbar at 360px introduced by your offsets.

## `TODO` map

| TODO | Done means |
|------|------------|
| `.tag` | `relative` + small `top`/`left` offsets |
| `.title` | `relative` + `text-shadow` optional + tiny negative `letter-spacing` (optional) |
