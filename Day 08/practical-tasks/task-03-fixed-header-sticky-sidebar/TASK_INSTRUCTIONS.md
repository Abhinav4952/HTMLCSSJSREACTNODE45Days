# Task 03 — Fixed Header + Sticky Sidebar

## Summary

Build an app-shell layout: `position: fixed` header and a **scrollable main column** (`.content`). The first element inside `.content` is a **sticky** title that should remain visible at the top of the scrolling column while you scroll the article text.


## `TODO` map

| TODO | Done means |
|------|------------|
| shell | `styles/main.css` completes layout rules per comments |
