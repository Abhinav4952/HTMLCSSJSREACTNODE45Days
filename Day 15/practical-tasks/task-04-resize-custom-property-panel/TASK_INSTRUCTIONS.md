# Task 04 — Resize + Custom Property Panel

## Summary

Build a **resizable** “log panel” using CSS `resize`, pair it with **`overflow`**, and drive visuals using **`--*` custom properties** declared on `:root`.

## Learning goals

- Use `resize` safely with scrolling content.
- Practice tokenized styling with custom properties.

## Provided files

- `index.html`, `styles/main.css`

## Prerequisites

- Day 15 theory: resize + transitions.

## What you will implement

Complete all `TODO(Day15-task04)` markers.

## Constraints

- No JavaScript.

## How to run and verify

### Manual checks

- [ ] Panel is resizable via drag affordance (desktop browsers; note limitations if any).
- [ ] Log content scrolls inside panel when smaller than content.
- [ ] At least two new `:root` tokens beyond `--panel-bg` / `--panel-border` are used.

## `TODO` map

| TODO | Done means |
|------|------------|
| CSS resize | Works with overflow |
| CSS tokens | New `:root` vars used |
| CSS hover shadow | Shadow transition only |
| HTML note | Explains resize mode + limitations |

## Submission checklist (Git)

- [ ] TODOs removed
