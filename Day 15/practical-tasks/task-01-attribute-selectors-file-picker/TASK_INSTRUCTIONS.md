# Task 01 — Attribute Selectors + File Picker Styling

## Summary

Practice **attribute selectors** on real `href` patterns and on `input[type="file"]` / `accept`—without adding classes for every variant.

## Learning goals

- Use `[attr^=…]`, `[attr$=…]`, and `[attr*=…]` intentionally.
- Style file inputs without breaking keyboard focus.

## Provided files

- `index.html`, `styles/main.css`

## Prerequisites

- Day 15 theory: attribute selectors.

## What you will implement

Complete all `TODO(Day15-task01)` markers.

## How to run and verify

### Manual checks

- [ ] Three link types are visually distinct (color/icon/weight—your choice) using **attribute selectors**, not per-link classes.
- [ ] File picker has improved styling + visible `:focus-visible` ring.
- [ ] `accept` substring rule applies to the file input.

## `TODO` map

| TODO | Done means |
|------|------------|
| HTML help sentence | Names the HTTPS selector strategy |
| CSS file input | Styled + focus-visible |
| CSS links | Three-way attribute selector styling |
| CSS accept | Archive-oriented accent |

## Submission checklist (Git)

- [ ] TODOs removed
