# Task 02 — Bracket + Dynamic Keys (Vitest)

## Summary

Practice **dynamic property access** with `pickOwn` and build objects from **entry arrays**.

## Provided files

- `package.json`, `.npmrc`, `src/keysLab.js`, `tests/keysLab.test.js`

## What you will implement

Complete all `// TODO(Day19-task02):` markers in `src/keysLab.js`.

## Constraints

- Use `Object.prototype.hasOwnProperty.call` (or `Object.hasOwn`) for own-key checks.
- Do not edit tests.

## How to run and verify

```bash
cd "Day 19/practical-tasks/task-02-bracket-and-dynamic-keys"
npm install
npm test
```

## `TODO` map

| Function | Done means |
|----------|------------|
| `pickOwn` | Own-key picking rules |
| `objectFromEntries` | Entry parsing + overwrite rules |

## Submission checklist (Git)

- [ ] `npm test` passes
- [ ] `package-lock.json` exists in this folder
