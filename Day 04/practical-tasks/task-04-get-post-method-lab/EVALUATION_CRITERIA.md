# Evaluation Criteria — Day 04 — Task 04 — GET vs POST Lab

## Scope

Graded: dual forms + conceptual explanation accuracy at intro level.

## Weighting (100 points)

| Category | Points | Description |
|----------|-------:|-------------|
| Correctness | 45 | Methods correct; controls labeled |
| Requirements coverage | 35 | Explanation length + content |
| Code quality | 20 | Section headings clarify which form is which |
| Readability | 10 | Writing clarity |

## Pass / fail gates

- Both forms missing or sharing a single form element wrapping everything incorrectly (each must be its own `<form>` unless using `form` attribute explicitly—default: two `<form>` elements).

## AI grading prompt

Verify two forms: GET search with q, POST report with two fields, explanatory section >=60 words, methods accurate.
