# Task 04 — Array Search + Slice (Vitest)

## Summary

Practice **tail slicing** and a linear **search** with explicit invalid-input rules.

## Provided files

- `package.json`, `.npmrc`, `src/searchLab.js`, `tests/searchLab.test.js`

## What you will implement

Complete all `// TODO(Day20-task04):` markers in `src/searchLab.js`.

## Constraints

- Do not edit tests.

## How to run and verify

```bash
cd "Day 20/practical-tasks/task-04-array-search-and-slice"
npm install
npm test
```

## `TODO` map

| Function | Done means |
|----------|------------|
| `tail` | Slice semantics + validation |
| `firstIndexGreaterThan` | Search semantics + validation |

## Submission checklist (Git)

- [ ] `npm test` passes
- [ ] `package-lock.json` exists in this folder
