# Task 03 — Reduce Ledger Lite (Vitest)

## Summary

Implement **`ledgerBalance`** using `reduce` to aggregate signed **credit/debit** rows.

## Provided files

- `package.json`, `.npmrc`, `src/reduceLab.js`, `tests/reduceLab.test.js`

## What you will implement

Complete `// TODO(Day20-task03):` in `src/reduceLab.js`.

## Constraints

- Must use `Array.prototype.reduce` at least once in `ledgerBalance`.
- Do not edit tests.

## How to run and verify

```bash
cd "Day 20/practical-tasks/task-03-reduce-ledger-lite"
npm install
npm test
```

## `TODO` map

| TODO | Done means |
|------|------------|
| `ledgerBalance` | Uses reduce + matches validation rules |

## Submission checklist (Git)

- [ ] `npm test` passes
- [ ] `package-lock.json` exists in this folder
